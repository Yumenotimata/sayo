import utest

import interpret
import utest

val tests = [interpret-tests]

fun main()

  tests.map fn(test)
    run-utest(test)
  // val program = Handle(Op("read", Lit(Unit)), [("read", Fn("_", Lit(Int(1))))])
  // eval(program)


  return ()